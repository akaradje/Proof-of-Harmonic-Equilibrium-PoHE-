[project]
name = "pohe-relay-node"
version = "0.0.1"
description = "PoHE Relay Node — seed issuer, VDF verifier, mint oracle"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
